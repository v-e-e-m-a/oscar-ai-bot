[project]
name = "oscar-ai-bot"
version = "3.1.0"

[tool.isort]
skip_glob = ["cdk.out/*", ".venv/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
